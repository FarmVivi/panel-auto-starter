<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fr.farmvivi.panel-auto-starter</groupId>
    <artifactId>panel-auto-starter-parent</artifactId>
    <version>3.3.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>PanelAutoStarter</name>
    <description>Parent POM for PanelAutoStarter multi-module project (Pterodactyl and Pelican panel support)</description>

    <modules>
        <module>panel-auto-starter-common</module>
        <module>panel-auto-starter-bungee</module>
        <module>panel-auto-starter-velocity</module>
    </modules>

    <properties>
        <!-- Bytecode cible : Java 21, la version exigee par Minecraft 1.21+.
             La CI compile avec un JDK plus recent, mais <release> garantit que
             le jar reste chargeable sur un serveur en Java 21. -->
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Configuration SonarCloud -->
        <sonar.projectKey>FarmVivi_panel-auto-starter</sonar.projectKey>
        <sonar.organization>farmvivi</sonar.organization>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
        <!-- Skip sonar analysis by default - enable with -Psonar profile -->
        <sonar.skip>true</sonar.skip>

        <!-- Dependency versions -->
        <adventure.version>5.2.0</adventure.version>
        <packetevents.version>2.13.0</packetevents.version>
        <gson.version>2.14.0</gson.version>
        <!-- bungeecord-api et bungeecord-config partagent la meme ligne de version -->
        <bungeecord.version>1.21-R0.4</bungeecord.version>
        <pterodactyl4j.version>2.BETA_142</pterodactyl4j.version>
        <velocity.version>4.0.0</velocity.version>
        <junit.version>6.1.2</junit.version>
        <snakeyaml.version>2.6</snakeyaml.version>
        <mockito.version>5.23.0</mockito.version>
    </properties>

    <repositories>
        <repository>
            <id>codemc</id>
            <name>CodeMC</name>
            <url>https://repo.codemc.io/repository/maven-releases/</url>
        </repository>
        <repository>
            <id>maven_central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2/</url>
        </repository>

        <repository>
            <id>papermc</id>
            <name>PaperMC</name>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>

        <repository>
            <id>mattmalec-repo</id>
            <name>Mattmalec Repository</name>
            <url>https://repo.mattmalec.com/repository/releases</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.github.retrooper</groupId>
                <artifactId>packetevents-api</artifactId>
                <version>${packetevents.version}</version>
                <scope>provided</scope>
            </dependency>
            <!-- Internal modules -->
            <dependency>
                <groupId>fr.farmvivi.panel-auto-starter</groupId>
                <artifactId>panel-auto-starter-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Adventure API -->
            <dependency>
                <groupId>net.kyori</groupId>
                <artifactId>adventure-api</artifactId>
                <version>${adventure.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- Adventure Text Serializer ANSI -->
            <dependency>
                <groupId>net.kyori</groupId>
                <artifactId>adventure-text-serializer-ansi</artifactId>
                <version>${adventure.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- Adventure Text Serializer Gson : sert a persister le MOTD des
                 serveurs sans perte. Le format legacy perdrait les degrades et
                 les couleurs hexadecimales, courants dans les MOTD. -->
            <dependency>
                <groupId>net.kyori</groupId>
                <artifactId>adventure-text-serializer-gson</artifactId>
                <version>${adventure.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- Gson est fourni par BungeeCord comme par Velocity : on le declare
                 en provided pour ne pas en embarquer une seconde copie dans le
                 jar shade. -->
            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>${gson.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Adventure Text Serializer Plain : sert a comparer un motif
                 d'ejection a du texte, sans avoir a depouiller des codes de
                 couleur a la main. -->
            <dependency>
                <groupId>net.kyori</groupId>
                <artifactId>adventure-text-serializer-plain</artifactId>
                <version>${adventure.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- Adventure Text Serializer Legacy -->
            <dependency>
                <groupId>net.kyori</groupId>
                <artifactId>adventure-text-serializer-legacy</artifactId>
                <version>${adventure.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- BungeeCord Config -->
            <dependency>
                <groupId>net.md-5</groupId>
                <artifactId>bungeecord-config</artifactId>
                <version>${bungeecord.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- Pterodactyl4J -->
            <dependency>
                <groupId>com.mattmalec</groupId>
                <artifactId>Pterodactyl4J</artifactId>
                <version>${pterodactyl4j.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- BungeeCord API : remplace waterfall-api, dont le projet est EOL
                 depuis mars 2024. Le module n'utilisait aucune API specifique a
                 Waterfall, uniquement du net.md_5.bungee.* standard. Au passage
                 on quitte un SNAPSHOT pour une release, ce qui rend le build
                 reproductible. -->
            <dependency>
                <groupId>net.md-5</groupId>
                <artifactId>bungeecord-api</artifactId>
                <version>${bungeecord.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Velocity API -->
            <dependency>
                <groupId>com.velocitypowered</groupId>
                <artifactId>velocity-api</artifactId>
                <version>${velocity.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- JUnit Jupiter -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- SnakeYAML : bungeecord-config le declare "optional", il n'est donc
                 pas transitif. A l'execution il est fourni par le proxy (BungeeCord
                 et Velocity l'embarquent tous les deux) ; en test il faut le fournir
                 nous-memes, sinon ConfigurationProvider.getProvider() renvoie null. -->
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Mockito for mocking -->
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.12.0</version>
            </plugin>
        </plugins>
    </reporting>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.4.0</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.12.0</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil pour l'analyse SonarQube -->
        <profile>
            <id>sonar</id>
            <properties>
                <sonar.skip>false</sonar.skip>
                <!-- JaCoCo coverage report -->
                <sonar.coverage.jacoco.xmlReportPaths>target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
            </properties>
            <build>
                <plugins>
                    <!-- JaCoCo for code coverage -->
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.8.15</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>report</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonarsource.scanner.maven</groupId>
                        <artifactId>sonar-maven-plugin</artifactId>
                        <version>5.7.0.6970</version>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <defaultGoal>clean package</defaultGoal>

        <pluginManagement>
            <!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) -->
            <plugins>
                <!-- clean lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#clean_Lifecycle -->
                <plugin>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <!-- default lifecycle, jar packaging: see https://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_jar_packaging -->
                <plugin>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.6</version>
                    <configuration>
                        <!-- Mockito est attache explicitement, plutot que de
                             s'auto-attacher a la JVM en cours. Cette
                             auto-attache echoue des que le chemin de classe
                             s'allonge, et le JDK la retirera de toute facon :
                             Mockito l'annonce dans un avertissement a chaque
                             execution. -->
                        <argLine>-javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>3.7.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.5.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.6.2</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>3.6.3</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>templating-maven-plugin</artifactId>
                    <version>3.1.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <!-- site lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#site_Lifecycle -->
                <plugin>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>3.22.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-project-info-reports-plugin</artifactId>
                    <version>3.9.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>3.3.1</version>
                    <configuration>
                        <autoVersionSubmodules>true</autoVersionSubmodules>
                        <tagNameFormat>@{project.version}</tagNameFormat>
                        <scmCommentPrefix xml:space="preserve">[RELEASE] </scmCommentPrefix>
                        <goals>install deploy site-deploy</goals>
                        <releaseProfiles>release</releaseProfiles>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!-- Pas de configuration exec ici pour ne pas interférer avec les modules -->
        <plugins/>
    </build>
</project>