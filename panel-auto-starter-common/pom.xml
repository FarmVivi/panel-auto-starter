<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>fr.farmvivi.panel-auto-starter</groupId>
        <artifactId>panel-auto-starter-parent</artifactId>
        <version>3.4.0-SNAPSHOT</version>
    </parent>

    <artifactId>panel-auto-starter-common</artifactId>
    <name>PanelAutoStarter-Common</name>
    <description>Common module providing shared API and utilities for Panel Auto Starter (Pterodactyl and Pelican)</description>

    <dependencies>
        <!-- PacketEvents : dependance FACULTATIVE, fournie par le plugin du meme
             nom installe sur le proxy. Portee « provided » : elle n'est pas
             embarquee, et le plugin doit se charger sans elle. -->
        <dependency>
            <groupId>com.github.retrooper</groupId>
            <artifactId>packetevents-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- MiniMessage : le format de mise en forme que Velocity emploie pour
             son propre MOTD, et donc celui que les administrateurs ecrivent
             deja. -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-minimessage</artifactId>
        </dependency>

        <!-- Codes couleur historiques : la forme « &b&l » que les
             administrateurs de serveurs ecrivent deja. -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-serializer-legacy</artifactId>
        </dependency>

        <!-- Adventure API -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-api</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Adventure Text Serializer Gson -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-serializer-gson</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Gson : fourni par le proxy a l'execution -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Adventure Text Serializer Plain -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-serializer-plain</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Adventure Text Serializer ANSI -->
        <dependency>
            <groupId>net.kyori</groupId>
            <artifactId>adventure-text-serializer-ansi</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- BungeeCord Config -->
        <dependency>
            <groupId>net.md-5</groupId>
            <artifactId>bungeecord-config</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Pterodactyl4J -->
        <dependency>
            <groupId>com.mattmalec</groupId>
            <artifactId>Pterodactyl4J</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Mockito for mocking -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- SnakeYAML : fourni par le proxy a l'execution, requis en test pour
             que YamlConfiguration soit utilisable -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>
            <!-- Résout le chemin du jar Mockito, que surefire attache en agent
                 plutôt que de le laisser s'auto-attacher à la JVM en cours. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
